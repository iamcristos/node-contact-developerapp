extends layout

block content
  h1= title
  p Welcome to #{title}
    div.well
        each service, i in services
            h2 #{service.name}
            p #{service.description}
            h4 #{service.price}  